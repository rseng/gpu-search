# https://github.com/pydata/xarray

```console
xarray/tests/test_cupy.py:def test_check_data_stays_on_gpu(toy_weather_data) -> None:
xarray/tests/test_cupy.py:    """Perform some operations and check the data stays on the GPU."""
xarray/tests/test_dataset.py:    on read. Modeled after nVIDIA's kvikio.
doc/user-guide/duckarrays.rst:* `Cupy <https://cupy.dev/>`_ - GPU support (see `cupy-xarray <https://cupy-xarray.readthedocs.io>`_),
doc/internals/interoperability.rst:- handle the data as any API-compliant numpy-like array type (e.g. sparse or GPU-backed),

```
