# https://github.com/CouncilDataProject/speakerbox

```console
speakerbox/main.py:        This also sets `no_cuda=True` on TrainerArguments.
speakerbox/main.py:        Default: False (use GPU if available)
speakerbox/main.py:        trainer_arguments_kws["no_cuda"] = True
speakerbox/main.py:    torch.cuda.empty_cache()
README.md:of the different dataset sizes. All models were fine-tuned using an NVIDIA GTX 1070 TI.
README.md:**Note:** this table can be reproduced in ~1 hour using an NVIDIA GTX 1070 TI by:
README.md:-   It is recommended to train with an NVidia GPU with CUDA available to speed up
paper/paper.md:All results reported are the average of five model training and evaluation trials for each of the different dataset sizes. All models were fine-tuned using an NVIDIA GTX 1070 TI.

```
