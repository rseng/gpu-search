# https://github.com/ExeClim/Isca

```console
postprocessing/mppnccombine.c:   ncclose(ncoutfile->ncfid); free(ncoutfile);
postprocessing/mppnccombine.c:      ncclose(ncinfile->ncfid); free(ncinfile); return(1);
postprocessing/mppnccombine.c:         ncclose(ncinfile->ncfid); free(ncinfile); return(1);
postprocessing/mppnccombine.c:         ncclose(ncinfile->ncfid); free(ncinfile); return(1);
postprocessing/mppnccombine.c:   ncclose(ncinfile->ncfid); free(ncinfile); return(ncinfileerror);

```
