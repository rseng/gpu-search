# https://github.com/LucaMalavolta/PyORBIT

```console
docs/multidimensional_gps/md_quasiperiodic.md:- GPU acceleration supported (instruction incoming)
docs/gaussian_process/quasiperiodic_kernel.md:- GPU acceleration supported (instruction incoming)
docs/running_pyorbit/tinygp_caveats.md:The `tinyGP` alternatives deliver the same results as their traditional counterparts (as far as I know). The main advantages over the traditional are faster loglikelihood computation and the support of GPU acceleration.

```
