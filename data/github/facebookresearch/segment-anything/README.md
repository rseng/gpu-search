# https://github.com/facebookresearch/segment-anything

```console
README.md:The code requires `python>=3.8`, as well as `pytorch>=1.7` and `torchvision>=0.8`. Please follow the instructions [here](https://pytorch.org/get-started/locally/) to install both PyTorch and TorchVision dependencies. Installing both PyTorch and TorchVision with CUDA support is strongly recommended.
scripts/amg.py:parser.add_argument("--device", type=str, default="cuda", help="The device to run generation on.")
segment_anything/automatic_mask_generator.py:            by the model. Higher numbers may be faster but use more GPU memory.
demo/README.md:sam.to(device='cuda')

```
