# https://github.com/lmcinnes/umap

```console
doc/faq.rst:Is there GPU or multicore-CPU support?
doc/faq.rst:In the future it is possible that GPU support may
doc/faq.rst:There is a UMAP implementation for GPU available in
doc/faq.rst:the NVIDIA RAPIDS cuML library, so if you need GPU
doc/parametric_umap.rst:* **batch_size:** ParametricUMAP in trained over batches of edges randomly sampled from the UMAP graph, and then trained via gradient descent.  ParametricUMAP defaults to a batch size of 1000 edges, but can be adjusted to a value that fits better on your GPU or CPU.

```
