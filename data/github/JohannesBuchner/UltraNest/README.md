# https://github.com/JohannesBuchner/UltraNest

```console
docs/performance.rst:GPU-acceleration
docs/performance.rst:which allows fast model evaluations on GPUs and CPUs.
ultranest/popstepsampler.py:Likelihood based on GPUs (model emulators based on neural networks,
tests/test_utils.py:    class FuncClass(object):
tests/test_utils.py:    mycaller = FuncClass()
HISTORY.rst:* add PopulationRandomWalkSampler: vectorized Gaussian random walks for GPU/JAX-powered likelihoods
examples/test.py:    parser.add_argument('-use_gpu', action='store_true')

```
