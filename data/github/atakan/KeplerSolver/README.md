# https://github.com/atakan/KeplerSolver

```console
docs/sol_kep.tex:around with it more easily, parallelize the code, port it to GPUs etc.
CF_kep_drift.c:	/* non-recursive version, more suitable to port to GPUS.

```
