# https://github.com/oliverphilcox/PolyBin3D

```console
README.md:The code supports GPU acceleration using JAX, which can be enabled using the `backend` argument in the `base` class, as demonstrated below.
README.md:                    backend='fftw', # backend for performing FFTs ('fftw' for cpu, 'jax' for gpu)
README.md:- JAX (for GPU acceleration, see [here](https://jax.readthedocs.io/en/latest/installation.html) for installation instructions.)

```
