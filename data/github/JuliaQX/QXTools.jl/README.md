# https://github.com/JuliaQX/QXTools.jl

```console
docs/src/basics.md:For more complex networks and output sampling methods we instead use the [QXContexts](https://github.com/JuliaQX/QXContexts.jl) package which provides more advanced sampling tools and supports distributed simulation as well as the use of GPU accelerators.
bin/qxrun.jl:        "--gpu", "-g"
bin/qxrun.jl:            help = "Use GPU if available"
bin/qxrun.jl:    use_gpu        = args["gpu"]
bin/qxrun.jl:                      use_gpu=use_gpu, max_amplitudes=number_amplitudes,
bin/qxrun.jl:                          use_gpu=use_gpu, max_amplitudes=number_amplitudes,

```
