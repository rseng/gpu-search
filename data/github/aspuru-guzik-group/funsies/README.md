# https://github.com/aspuru-guzik-group/funsies

```console
src/funsies/config.py:    if a complex workflow requires GPUs for certain jobs, those jobs would be
src/funsies/config.py:    setup with option `queue="gpu"` and workers on the nodes with available
src/funsies/config.py:    GPUs would be instantiated with `funsies worker gpu`. Then, only worker
src/funsies/config.py:    processes in the GPU queue would execute the GPU jobs."""

```
