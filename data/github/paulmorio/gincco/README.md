# https://github.com/paulmorio/gincco

```console
README.md:If you have a CUDA enabled GPU, use the below command. Otherwise follow instructions on the PyTorch website.
gincco/random_gincco_tcga.py:if torch.cuda.is_available():
gincco/random_gincco_tcga.py:    device = torch.device('cuda')
gincco/gincco_metabric.py:if torch.cuda.is_available():
gincco/gincco_metabric.py:    device = torch.device('cuda')
gincco/baseline_metabric_w_auc.py:if torch.cuda.is_available():
gincco/baseline_metabric_w_auc.py:    device = torch.device('cuda')
gincco/baseline_metabric.py:if torch.cuda.is_available():
gincco/baseline_metabric.py:    device = torch.device('cuda')
gincco/batch_random_tcga.py:if torch.cuda.is_available():
gincco/batch_random_tcga.py:    device = torch.device('cuda')
gincco/baseline_tcga.py:if torch.cuda.is_available():
gincco/baseline_tcga.py:    device = torch.device('cuda')
gincco/baseline_tcga_w_auc.py:if torch.cuda.is_available():
gincco/baseline_tcga_w_auc.py:    device = torch.device('cuda')
gincco/CustomizedLinear.py:        # won't be converted when e.g. .cuda() is called. You can use
gincco/gincco_tcga.py:if torch.cuda.is_available():
gincco/gincco_tcga.py:    device = torch.device('cuda')
gincco/batch_random_metabric.py:if torch.cuda.is_available():
gincco/batch_random_metabric.py:    device = torch.device('cuda')
gincco/random_gincco_metabric.py:if torch.cuda.is_available():
gincco/random_gincco_metabric.py:    device = torch.device('cuda')

```
