# https://github.com/MD-Studio/cerise

```console
docs/source/requirements.rst:        e.g. GPUs, by submitting to a particular queue, or by specifying a
docs/source/configuration.rst:you need to specify additional scheduler options to e.g. select a GPU node, you
docs/source/configuration.rst:can do so using e.g. ``scheduler-options: "-C TitanX --gres=gpu:1"``. Ideally,
CHANGELOG.rst:  * Extra scheduler options (e.g. to select a GPU node)

```
