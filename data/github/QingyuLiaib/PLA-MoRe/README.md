# https://github.com/QingyuLiaib/PLA-MoRe

```console
training.py:    total_preds = torch.Tensor().cuda()
training.py:    total_labels = torch.Tensor().cuda()
training.py:    device = torch.device('cuda:0' if torch.cuda.is_available() else "cpu")

```
