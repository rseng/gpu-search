# https://github.com/nayu0419/stMMR

```console
stMMR/train_model.py:                device = torch.device('cuda:0' if torch.cuda.is_available() else 'cpu')):
stMMR/train_model.py:                device = torch.device('cuda:0' if torch.cuda.is_available() else 'cpu')):
stMMR/process.py:    torch.cuda.manual_seed(seed)
stMMR/process.py:    torch.cuda.manual_seed_all(seed)

```
