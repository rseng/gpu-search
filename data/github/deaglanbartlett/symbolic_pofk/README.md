# https://github.com/DeaglanBartlett/symbolic_pofk

```console
symbolic_pofk/pytorch/syrenhalofit.py:# Check if a GPU is available and set the device
symbolic_pofk/pytorch/syrenhalofit.py:device = torch.device("cuda" if torch.cuda.is_available() else "cpu")
symbolic_pofk/pytorch/linear.py:# Check if a GPU is available and set the device
symbolic_pofk/pytorch/linear.py:device = torch.device("cuda" if torch.cuda.is_available() else "cpu")
symbolic_pofk/pytorch/linear.py:    This function provides a much faster computation of the growth factor, especially when using GPUs.
symbolic_pofk/pytorch/linear_new.py:# Check if a GPU is available and set the device
symbolic_pofk/pytorch/linear_new.py:device = torch.device("cuda" if torch.cuda.is_available() else "cpu")
symbolic_pofk/pytorch/syren_new.py:device = torch.device("cuda" if torch.cuda.is_available() else "cpu")

```
