# https://github.com/epruesse/SINA

```console
include/spdlog/fmt/bundled/format.h:# define FMT_CUDA_VERSION (__CUDACC_VER_MAJOR__ * 100 + __CUDACC_VER_MINOR__)
include/spdlog/fmt/bundled/format.h:# define FMT_CUDA_VERSION 0
include/spdlog/fmt/bundled/format.h:// For Intel's compiler and NVIDIA's compiler both it and the system gcc/msc
include/spdlog/fmt/bundled/format.h:      (!(FMT_ICC_VERSION || FMT_CUDA_VERSION) || \
include/spdlog/fmt/bundled/format.h:       FMT_ICC_VERSION >= 1500 || FMT_CUDA_VERSION >= 700)
include/spdlog/fmt/bundled/format.h:    FMT_CUDA_VERSION == 0 && \

```
